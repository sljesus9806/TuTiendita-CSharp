<UserControl x:Class="TuTiendita.ProveedoresUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="🏭 Gestión de Proveedores" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="Administra tus proveedores y órdenes de compra"
                       FontSize="13" Foreground="#7F8C8D" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Search and Actions -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Search Box -->
            <Border Grid.Column="0" Background="White" BorderBrush="#BDC3C7" BorderThickness="1"
                    CornerRadius="5" Padding="10" Margin="0,0,10,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="🔍" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox x:Name="txtBuscar" Grid.Column="1" BorderThickness="0"
                             VerticalAlignment="Center" FontSize="14"
                             TextChanged="TxtBuscar_TextChanged">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <VisualBrush Stretch="None" AlignmentX="Left">
                                                    <VisualBrush.Visual>
                                                        <TextBlock Text="Buscar proveedor por nombre, RFC o contacto..."
                                                                   Foreground="#95A5A6" FontSize="14"/>
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </Grid>
            </Border>

            <!-- Action Buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="btnNuevoProveedor" Content="➕ Nuevo Proveedor" Width="160" Height="40"
                        Background="#27AE60" Foreground="White" FontWeight="Bold" BorderThickness="0"
                        Margin="0,0,10,0" Click="BtnNuevoProveedor_Click" Cursor="Hand"/>
                <Button x:Name="btnActualizar" Content="🔄 Actualizar" Width="120" Height="40"
                        Background="#3498DB" Foreground="White" FontWeight="Bold" BorderThickness="0"
                        Click="BtnActualizar_Click" Cursor="Hand"/>
            </StackPanel>
        </Grid>

        <!-- Suppliers DataGrid -->
        <Border Grid.Row="2" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="10">
            <DataGrid x:Name="dgProveedores" AutoGenerateColumns="False"
                      IsReadOnly="True" SelectionMode="Single"
                      CanUserAddRows="False" CanUserDeleteRows="False"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      RowHeight="45" AlternatingRowBackground="#F8F9FA">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="200"/>
                    <DataGridTextColumn Header="RFC" Binding="{Binding RFC}" Width="120"/>
                    <DataGridTextColumn Header="Contacto" Binding="{Binding Contacto}" Width="150"/>
                    <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}" Width="120"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180"/>
                    <DataGridTextColumn Header="Dirección" Binding="{Binding Direccion}" Width="*"/>
                    <DataGridTemplateColumn Header="Acciones" Width="300">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="📦 Nueva Orden" Width="110" Height="30" Margin="5,0"
                                            Background="#E67E22" Foreground="White" FontSize="11"
                                            BorderThickness="0" Click="BtnNuevaOrden_Click" Cursor="Hand"
                                            Tag="{Binding Id}"/>
                                    <Button Content="✏️ Editar" Width="80" Height="30" Margin="5,0"
                                            Background="#3498DB" Foreground="White" FontSize="11"
                                            BorderThickness="0" Click="BtnEditar_Click" Cursor="Hand"
                                            Tag="{Binding Id}"/>
                                    <Button Content="🗑️ Eliminar" Width="80" Height="30" Margin="5,0"
                                            Background="#E74C3C" Foreground="White" FontSize="11"
                                            BorderThickness="0" Click="BtnEliminar_Click" Cursor="Hand"
                                            Tag="{Binding Id}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
